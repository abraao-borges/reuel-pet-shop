# Common configuration shared across all profiles

# Security (Owner Admin)
owner.username=${OWNER_USERNAME:owner}
owner.password=${OWNER_PASSWORD:change-me-now}

# Seeders
app.seed.orders=${APP_SEED_ORDERS:true}
app.seed.products=${APP_SEED_PRODUCTS:true}

# Payments
abacatepay.api.key=${ABACATEPAY_API_KEY:abc_dev_F5HLDKByfY32WYdWmmM1MqMS}
abacatepay.webhook.secret=${ABACATEPAY_WEBHOOK_SECRET:YOUR_WEBHOOK_SECRET}

# JPA / Hibernate (shared)
spring.jpa.hibernate.ddl-auto=update
